[Settings]

# time of holding the limit order (in minute)
time = "5"

# limit in $
limit = "300000"

# reset coefficient in %
cf_update = "0.9"

# distance to density  in %
cf_distance = "0.05"

# time of repeated notification in case of a rebound from the price (in minute)
time_resend = "5"